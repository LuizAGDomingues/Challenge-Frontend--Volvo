interface PaginationMobileProps {
  total: number,
  onClick: (i: number) => void
}

function PaginationMobile({}: PaginationMobileProps) {
  return <div>PaginationMobile</div>
}

export default PaginationMobile
